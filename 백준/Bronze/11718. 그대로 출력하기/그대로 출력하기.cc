#include <bits/stdc++.h>
using namespace std;

int n;

void solve() {
  string str;
  getline(cin, str);
  cout << str << "\n";
}

int main() {
  ios::sync_with_stdio(false), cin.tie(0), cout.tie(0); /* FastIO */

  int T = 102;
  while (T--) {
    solve();
  }
}
