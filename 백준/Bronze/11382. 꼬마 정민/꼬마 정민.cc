#include <bits/stdc++.h>
using namespace std;

long long a, b, c;

int main() {
  ios::sync_with_stdio(false), cin.tie(0), cout.tie(0); /* FastIO */

  cin >> a >> b >> c;
  cout << a + b + c << "\n";
}
